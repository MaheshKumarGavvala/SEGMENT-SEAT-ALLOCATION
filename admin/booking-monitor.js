(() => {const $=s=>document.querySelector(s);let rows=[];const api=window.SmartSegmentAPI;const esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]));function render(){const q=$("#search").value.toLowerCase();const x=rows.filter(r=>[r.code,r.route,r.status,r.bus,r.passenger,r.mobile].join(" ").toLowerCase().includes(q));$("#list").innerHTML=x.length?x.map(r=>`<div class="booking-row"><div><b>${esc(r.code)}</b><small>${esc(new Date(r.date).toLocaleDateString('en-IN'))} · ${esc(r.status)}</small></div><div><b>${esc(r.route)}</b><small>${esc(r.passenger)} · ${esc(r.mobile)}</small></div><div><strong>₹${Number(r.fare).toLocaleString('en-IN')}</strong><small>${esc(r.bus||'')} · ${r.segments.map(s=>'Seat '+s.seat).join(', ')}</small></div><span class="pill">${esc(r.payment)}</span></div>`).join(""):"<div class='empty'>No bookings match your search.</div>"}async function init(){try{const r=await api.get('/api/admin/bookings');rows=r.data||[];render()}catch(e){alert(e.message);location.href='../auth/auth/login/login.html'}}$("#search").oninput=render;init()})();
